#include <fstream>
#include <iostream>
#include <string>
#include <vector>

auto read_input(std::ifstream& in) {
    return "";
}

long long solve(auto input) {
    return 0ll; 
}

int main() {
    std::ios::sync_with_stdio(0);
    std::cin.tie(0);
}
